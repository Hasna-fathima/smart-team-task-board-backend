version: '3.8'

services:
  backend:
    build: ./backend
    ports:
      - "5000:5000"
    environment:
      - PORT=5000
      - NODE_ENV=production
      - MONGODB_URI=mongodb+srv://hf765729_db_user:fM2eOGgnmWoJoiOF@cluster0.miifce5.mongodb.net
      - JWT_SECRET=supersecret_jwt_key_smart_task_board_2026
      - CLIENT_URL=http://localhost:3000
    restart: always

  frontend:
    build: ./MERN-Assignment-frontend
    ports:
      - "3000:3000"
    environment:
      - NEXT_PUBLIC_API_URL=http://localhost:5000/api
      - NEXT_PUBLIC_SOCKET_URL=http://localhost:5000
    depends_on:
      - backend
    restart: always
